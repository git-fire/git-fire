# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Fixed

- **`--init` + `--config`:** `git-fire --init` now writes the example config to the path given by `--config` when set, instead of always using the default user config path.

## [0.1.0-beta] — _release date TBD_

Beta release: multi-repo scan, checkpoint/push flows, registry, TUI selector (`--fire`), and safety rails. See README for known limitations (`--backup-to`, plugin CLI auto-loading). Set the date to match the tag when publishing.

## [0.1.0-alpha] — _release date TBD_

Alpha release: multi-repo scan, checkpoint/push flows, registry, TUI selector (`--fire`), and safety rails. See README for known limitations (`--backup-to`, plugin CLI auto-loading). Set the date to match the tag when publishing.

[Unreleased]: https://github.com/git-fire/git-fire/compare/v0.1.0-beta...HEAD
[0.1.0-beta]: https://github.com/git-fire/git-fire/releases/tag/v0.1.0-beta
[0.1.0-alpha]: https://github.com/git-fire/git-fire/releases/tag/v0.1.0-alpha
