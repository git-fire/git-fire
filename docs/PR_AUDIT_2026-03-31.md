# PR Audit - 2026-03-31 (Last 6 Hours)

This audit verifies merged PRs in the last 6 hours, maps likely agent/workflow provenance, and checks docs claims against current code behavior on `origin/main`.

## In-Window PRs Reviewed

- `#21` `fix(ui): correct layout overflow causing fire/title to clip in small terminals`
- `#22` `docs: restructure OSS-facing docs and README flow`
- `#23` `feat(ui): fire animation toggle with persistence and small-screen suppression`
- `#24` `fix(ui): warn when overflow indicators are hidden in small terminals`
- `#25` `feat(ui): add selectable TUI color profiles`
- `#26` `chore(repo): migrate references to git-fire/git-fire`
- `#28` `docs: alpha readiness, contribution policy, and PR template`
- `#29` `docs: move TUI screenshot higher and optimize GitHub rendering`
- `#31` `docs: tighten alpha docs and add README TUI screenshot`
- `#32` `docs: reposition git-fire docs for daily, agentic, and security workflows`
- `#33` `chore(docs): trim validation bloat for OSS release`
- `#34` `feat(executor): parallel push execution with worker concurrency`

## Provenance Check (Cursor/Claude/Other)

Confidence levels are based on PR body markers and transcript evidence.

- `#21`: likely Claude Code authored (high confidence, PR body marker: "Generated with Claude Code").
- `#23`: likely Claude Code authored (high confidence, PR body marker: "Generated with Claude Code").
- `#22`, `#26`, `#28`, `#29`, `#31`, `#32`, `#33`, `#34`: evidence of Cursor agent workflow involvement in local agent transcripts and PR automation logs (medium-high confidence).
- No definitive evidence that random/manual edits bypassed all agent workflows for the docs-only PRs in this window.

## Docs Truth Check vs Code

### Verified Correct

- README statements about:
  - `--backup-to` being exposed but not implemented (`cmd/root.go` returns explicit error),
  - plugin internals existing while default CLI auto-loading is not wired yet,
  - configurable push worker concurrency and TUI-related options.

### Corrected in this follow-up

1. `docs/PROJECT_OVERVIEW.md`
   - Fixed stale "sequential push execution" wording.
   - Fixed stale "per-host cap 2" wording.
   - Updated to match current behavior: parallel worker execution + host-based rate limiting.

2. Validation source-of-truth flow
   - `docs/REQUIREMENTS_VALIDATION.md` contained historical assertions that are no longer reliable as canonical current status.
   - Added explicit historical-snapshot disclaimer at top of `docs/REQUIREMENTS_VALIDATION.md`.
   - Updated `docs/README.md` to point canonical behavior to `README.md`, `docs/PROJECT_OVERVIEW.md`, and `GIT_FIRE_SPEC.md`.

## Net Result

- The docs now route readers to accurate current behavior documents.
- Historical validation artifacts remain available without being presented as current canonical truth.
