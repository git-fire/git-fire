# PR Audit - 2026-03-31 (Last 6 Hours)

This audit verifies merged PRs in the last 6 hours, maps agent/workflow provenance, and checks docs claims against current behavior on `origin/main`.

## PR Matrix (Merged in Window)

Severity labels:
- `ok`: no docs correctness issue found
- `stale`: docs are outdated but not fundamentally wrong
- `incorrect`: docs state behavior that contradicts current code
- `unclear`: wording can mislead due to mixed or ambiguous scope

| PR | Type | Docs impact | Key claim/theme | Severity | Notes |
|---|---|---|---|---|---|
| `#21` | ui code | indirect | small-terminal rendering fixes | `ok` | No direct docs drift introduced. |
| `#22` | docs | direct | OSS docs restructure, plugin wording | `ok` | Subsequent updates resolved accidental mixed scope. |
| `#23` | ui+config | indirect | fire toggle + persisted config | `ok` | README claims match behavior. |
| `#24` | ui code | indirect | hidden-overflow warning | `ok` | Behavior-specific; no conflicting docs found. |
| `#25` | ui+config | indirect | color profiles + settings row | `ok` | README/docs claims align with config + UI code. |
| `#26` | repo-wide chore | direct | org path migration | `ok` | References aligned to `git-fire/git-fire`. |
| `#28` | docs | direct | alpha docs + overview docs | `stale` | Later PRs superseded portions of wording. |
| `#29` | docs | direct | screenshot position/asset | `ok` | Render/docs-only update remains valid. |
| `#31` | docs | none effective | alpha docs tightening | `unclear` | Merged with empty file delta at merge time. |
| `#32` | docs + mixed | direct | repositioning docs + clarified limits | `unclear` | Mixed-scope history noted; now merged and active on `main`. |
| `#33` | docs chore | direct | trim validation artifacts/noise | `stale` | Introduced "active status anchor" wording that drifted later. |
| `#34` | executor code | indirect | push worker concurrency | `incorrect` (downstream docs) | Exposed stale claim in `docs/PROJECT_OVERVIEW.md` about sequential pushes/per-host `2`. |

## Provenance Mapping (Agent/CLI Flow)

Confidence levels are based on PR body markers and local transcript evidence.

| PR | Provenance classification | Confidence | Evidence |
|---|---|---|---|
| `#21` | Claude CLI associated | High | PR body marker: "Generated with Claude Code". |
| `#22` | Cursor agent associated | High | Transcript and PR automation trail. |
| `#23` | Claude CLI associated | High | PR body marker: "Generated with Claude Code". |
| `#24` | Mixed/uncertain (agent-assisted likely) | Medium | No explicit body marker in final merge view. |
| `#25` | Cursor/agent-associated lineage | Medium | Transcript evidence from prior fork/worktree PR flow. |
| `#26` | Cursor agent associated | High | Transcript shows branch/commit/PR creation workflow. |
| `#28` | Cursor agent associated | High | Transcript includes PR creation + follow-up edits. |
| `#29` | Cursor agent associated | High | Transcript includes PR creation + commit info. |
| `#31` | Cursor agent associated | High | Transcript includes PR creation + follow-up comment flow. |
| `#32` | Cursor agent associated (mixed-scope evolution) | High | Transcript records open-PR scope adjustments and follow-up fixes. |
| `#33` | Cursor agent associated | High | Transcript includes PR creation then merge acknowledgment. |
| `#34` | Cursor agent associated | Medium-High | Transcript references open-state handling and non-merge guardrails before merge. |

No evidence found that random/manual edits bypassed all agent workflows for the docs-focused PRs in this window.

## PR #32 Status (Explicit)

- `#32` is merged to `main` and is now the head baseline in this audit window.
- `#32` had mixed-scope history (docs plus adjacent runtime/config touchpoints during review lifecycle), so this audit treated it as a high-risk docs-truth candidate.
- Current `main` validation confirms `#32` user-facing claims around `--backup-to` and plugin runtime wiring are accurate.

## Docs Truth Check vs Current Code

Validated against:
- `cmd/root.go`
- `internal/executor/runner.go`
- `internal/executor/ratelimit.go`
- `internal/config/defaults.go`
- `internal/config/loader.go`
- `internal/plugins/loader.go`

### Verified Correct

- `README.md` correctly states:
  - `--backup-to` exists but is not implemented (explicit runtime error path),
  - plugin internals exist while default CLI auto-loading is not wired yet,
  - push worker concurrency is configurable via config.

### Corrections Applied in This Follow-up

1. `docs/PROJECT_OVERVIEW.md`
   - Replaced stale sequential push model text.
   - Replaced stale "per-host cap 2" statement.
   - Updated wording to match parallel worker execution + host-based rate limiting.

2. Validation source-of-truth routing
   - `docs/REQUIREMENTS_VALIDATION.md` now explicitly labeled as historical snapshot.
   - `docs/README.md` now points canonical behavior readers to:
     - `README.md`
     - `docs/PROJECT_OVERVIEW.md`
     - `GIT_FIRE_SPEC.md`

## Net Result

- Docs now route readers to code-aligned current behavior sources.
- Historical validation artifacts remain available without being presented as canonical runtime truth.
